# 实现路线图

本路线图把产品优先级转化为可执行工程阶段。

总原则：

> 先完成“长录播 → 去/压静音 → 自动字幕 → 可审阅粗剪”的 P0 闭环，再扩展语义剪辑、知识可视化和多模态导演能力。

---

# P0：录播自动清理与字幕

## P0.0 仓库与工程骨架

### 目标

建立可以持续迭代、跨平台运行和测试的基础工程。

### 任务

- 选择 Python 版本和依赖管理方式；
- 建立 `src/` 布局；
- 建立 CLI 入口；
- 建立配置加载与校验；
- 建立结构化日志；
- 建立项目目录和状态文件；
- 建立单元测试与集成测试目录；
- 建立 Windows、Ubuntu、macOS 的基础 CI；
- 编写 FFmpeg 可用性检查；
- 增加示例配置和最小演示命令。

### 建议目录

```text
video-helper/
├── src/video_helper/
│   ├── cli/
│   ├── config/
│   ├── ingest/
│   ├── audio/
│   ├── speech/
│   ├── transcription/
│   ├── planning/
│   ├── timeline/
│   ├── subtitles/
│   ├── rendering/
│   ├── qc/
│   └── project/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
├── docs/
├── examples/
├── pyproject.toml
└── README.md
```

### 完成标准

- `video-helper --help` 可运行；
- 测试框架可运行；
- CLI 能创建空项目；
- FFmpeg 缺失时给出明确错误；
- Windows 中文路径测试通过。

---

## P0.1 媒体导入与代理文件

### 目标

可靠导入一整段录播并建立项目。

### 任务

- 使用媒体探测工具读取元数据；
- 检查音视频流；
- 检查可解码性；
- 生成 `project.json`；
- 生成 `media.json`；
- 计算输入哈希；
- 生成低分辨率代理视频；
- 处理旋转元数据；
- 支持取消和恢复；
- 对损坏文件提供清晰诊断。

### 完成标准

- 30 分钟、60 分钟和 120 分钟样本均可导入；
- 原始视频不被修改；
- 重复导入相同文件可命中缓存；
- 代理视频音画同步。

---

## P0.2 音频提取、VAD 与静音区间

### 目标

稳定识别语音与无语音区间。

### 任务

- 提取标准化分析音轨；
- 建立响度检测基线；
- 接入可替换 VAD 后端；
- 生成原始语音区间；
- 实现区间合并；
- 实现前后缓冲；
- 实现短停顿保留；
- 实现长空白压缩候选；
- 输出置信度和检测来源；
- 实现保守、标准、激进预设；
- 添加合成音频测试夹具。

### 完成标准

- 短停顿不会被默认删除；
- 语音起始和结尾不会被系统性截断；
- 空调、键盘等常见背景声样本有可接受表现；
- 低置信度区间默认保留并进入审阅报告。

---

## P0.3 ASR 与原始字幕

### 目标

为整段原始录播生成可追溯的转录和时间戳。

### 任务

- 定义 Transcriber 接口；
- 接入首个本地 ASR 后端；
- 支持句级时间戳；
- 支持词级时间戳；
- 自动语言或显式语言配置；
- 中文标点恢复；
- 术语词表；
- 输出置信度；
- 保存模型和参数信息；
- 生成 `transcript.json/txt/md`；
- 建立中英混合测试样本。

### 完成标准

- 一小时视频可完整转录；
- 时间戳单调递增且不越界；
- 低置信度片段可定位；
- 相同输入和配置重复运行可复用缓存。

---

## P0.4 剪切计划与时间轴映射

### 目标

把检测结果转化为可审阅、可执行且可逆的剪辑计划。

### 任务

- 定义 `keep/compress/drop` 数据模型；
- 生成完整覆盖原始时间轴的决策列表；
- 删除首尾明显空白；
- 压缩长无语音区间；
- 防止碎片化；
- 防止过密切口；
- 实现锁定和人工覆盖；
- 输出预计节省时长；
- 实现原始时间到成片时间的映射；
- 实现反向映射；
- 使用整数时间单位；
- 建立随机区间属性测试和长视频漂移测试。

### 完成标准

- 剪切计划无重叠、无间隙并覆盖完整时间轴；
- 正向与反向映射误差在定义范围内；
- 被删除区间可恢复；
- 60 分钟以上视频无明显累计漂移。

---

## P0.5 字幕重映射与断句

### 目标

生成与粗剪成片严格同步的字幕。

### 任务

- 将原始转录映射到成片时间轴；
- 处理跨越删除区间的转录片段；
- 按停顿和语义断句；
- 拆分过长字幕；
- 合并过短字幕；
- 实现最小/最大显示时长；
- 输出 SRT；
- 输出 ASS；
- 输出结构化字幕 JSON；
- 增加字幕文本修正入口；
- 建立字幕时间轴校验。

### 完成标准

- 字幕与成片同步；
- 字幕无负时长、无逆序、无越界；
- 删除长静音后，后续字幕无累计偏移；
- 字幕文件可被主流播放器读取。

---

## P0.6 预览、渲染与音频切口

### 目标

按照剪切计划可靠生成粗剪视频和字幕版本。

### 任务

- 设计 FFmpeg 执行层；
- 生成快速低清预览；
- 生成正式粗剪视频；
- 输出内嵌字幕版本；
- 输出无字幕净版；
- 对音频切口执行短交叉淡化；
- 检测和避免切口爆音；
- 保持原始帧率和分辨率；
- 支持硬件编码和软件回退；
- 渲染进度与取消；
- 渲染中断恢复；
- 局部重新渲染策略预留接口。

### 完成标准

- 输出文件可播放；
- 音画同步；
- 切口无普遍性爆音；
- 预览与正式渲染使用同一剪切计划；
- 软件编码路径在三大桌面系统可运行。

---

## P0.7 审阅、质量检查与一键运行

### 目标

形成真正可用的 P0 端到端体验。

### 任务

- `video-helper inspect` 输出剪切摘要；
- 输出所有删除和压缩区间；
- 标记低置信度区间；
- 支持通过配置文件修改决策；
- 生成 QC 报告；
- 检查字幕越界；
- 检查黑帧；
- 检查异常静音；
- 检查音画时长；
- 检查时间轴漂移；
- 实现 `video-helper run`；
- 实现 `--resume` 和 `--dry-run`；
- 提供示例项目和使用文档。

### P0 总体验收

使用一段至少 60 分钟的真实录播执行：

```bash
video-helper run input.mp4 --preset conservative
```

必须得到：

```text
rough_cut.mp4
rough_cut_subtitled.mp4
subtitles.srt
subtitles.ass
transcript.md
cut_plan.json
timeline_map.json
qc_report.md
```

且满足：

- 自动压缩明显无语音部分；
- 语音内容无普遍性截断；
- 字幕可读且同步；
- 所有剪切可解释和恢复；
- 中断后可继续；
- Windows 环境可完成完整流程。

---

# P1：可审阅的智能粗剪

## P1.0 本地审阅界面

- 视频播放器；
- 音频波形；
- 字幕和转录；
- 时间轴区间；
- 切口前后预览；
- 保留、压缩、删除切换；
- 锁定与恢复；
- 局部重新渲染。

## P1.1 文本式视频编辑

- 删除文字同步删除视频；
- 选择文本播放对应画面；
- 修改字幕；
- 批量接受建议；
- 多版本剪切方案比较。

## P1.2 语言级剪辑建议

- 重录检测；
- 重新起句检测；
- 重复语义检测；
- 填充词检测；
- 口误候选；
- 前提、结论和指代完整性检查；
- 所有语义删除默认等待人工确认。

## P1.3 音频与字幕增强

- 降噪；
- 响度标准化；
- EQ 与动态压缩；
- 底噪连续性；
- 逐词高亮字幕；
- 术语自动纠错；
- 字幕模板。

## P1.4 内容归纳

- 摘要；
- 核心论点；
- 章节与时间戳；
- 标题、简介和标签；
- 发布元数据。

### P1 完成标准

用户能够在一个本地界面内完成：

```text
导入 → 自动粗剪 → 文本审阅 → 字幕校对 → 音频增强 → 导出
```

---

# P2：知识视频生产系统

## P2.0 文稿对齐

- 导入原稿；
- 对齐实际口播；
- 标记漏读、增补、改述和重排；
- 用原稿校正术语和章节。

## P2.1 知识结构化

- 定义、命题、案例、结论和引用提取；
- 知识节点；
- 因果、组成、对比、反馈和时间关系；
- 知识图谱；
- 节点时间戳。

## P2.2 动态视觉计划

- 流程图；
- 节点网络；
- 因果循环；
- 思想史时间轴；
- 对比卡片；
- 节点逐步展开；
- 当前概念高亮；
- 程序化视频引擎集成。

## P2.3 素材与 B-roll

- 自动标记需要辅助画面的段落；
- 生成搜索关键词；
- 本地素材库；
- 授权记录；
- 素材时间轴建议；
- 人工确认后合成。

## P2.4 多平台版本

- 横屏长视频；
- 竖屏短视频；
- 自动重新构图；
- 精彩片段；
- 预告；
- 多语言字幕；
- 封面文案。

### P2 完成标准

一段录播和一份文稿能够生成：

```text
完成粗剪的知识视频
+ 章节
+ 知识图谱
+ 动态视觉计划
+ 横屏/竖屏输出
+ 短视频候选
```

---

# P3：多模态 AI 导演

## P3.0 长视频多模态理解

- 镜头级采样；
- 人脸、手势和屏幕内容；
- 视觉质量分析；
- 音频、文本和画面联合理解。

## P3.1 导演级决策

- 真人画面、B-roll 和动态图形之间的自动切换；
- 视觉连续性优化；
- 节奏和注意力曲线；
- 多种剪辑方案；
- 自动重组叙事；
- 解释与回滚。

## P3.2 生成式素材

- 插图；
- 动态背景；
- 短视频素材；
- 音乐和音效；
- 多语言配音；
- 可选口型同步。

## P3.3 风格学习与发布反馈

- 学习用户剪辑选择；
- 学习字幕和动画风格；
- 接入完播率和跳出点；
- 生成改进建议；
- 可查看、导出、重置个人风格配置。

### P3 完成标准

系统能够基于同一素材生成多个可解释、可编辑、可回滚的导演方案，并在用户授权下利用历史选择和发布反馈持续优化。

---

# 开发顺序约束

1. P0.0—P0.7 按依赖顺序推进；
2. P0 总体验收通过前，P2/P3 只允许做接口预留和研究验证；
3. P1 不得绕过 P0 的剪切计划与时间轴映射；
4. P2 动画、字幕和章节必须共享统一成片时间轴；
5. P3 只能生成候选决策，最终仍由确定性 Planner、Timeline 和 Renderer 执行；
6. 每个阶段都要产生可运行增量，避免长期停留在不可验证的基础设施建设。

# 首个 Codex 任务建议

首次拉取仓库后，建议向 Codex 提交以下目标：

> 阅读 `README.md`、`docs/PRODUCT_SPEC.md`、`docs/ARCHITECTURE.md`、`ROADMAP.md` 和 `AGENTS.md`。仅实现 P0.0：建立 Python 项目骨架、CLI、配置模型、项目初始化、结构化日志、FFmpeg 诊断和基础测试。不要提前实现 ASR、VAD、UI 或 P1 以上功能。完成后运行测试并更新路线图状态。